#Reload sxhkd 
shift + Escape
	pkill -USR1 -x sxhkd
# Reload i3
super + r 
    i3-msg reload
    # i3-msg reload  && i3-msg restart
#Terminal 
super + Return
    alacritty 
# browser
super + b
    waterfox
# firefox
super + shift + b
    ~/scripts/browser.sh
    # flatpak run net.waterfox.waterfox
#file manger gui 
super + shift + Return
    thunar 
#xkill 
super + x 
	xkill
#Vol up 
XF86AudioRaiseVolume
   ~/scripts/volume.sh up 
   # ~/scripts/volume.sh up && pkill -RTMIN+10 i3blocks
#Vol down 
XF86AudioLowerVolume
   ~/scripts/volume.sh down 
   # ~/scripts/volume.sh down && pkill -RTMIN+10 i3blocks
#vol toggle 
XF86AudioMute
   ~/scripts/volume.sh mute 
# vol up
super + Right
   ~/scripts/volume.sh up
# vol down
super + Left
   ~/scripts/volume.sh down 
# mic toggle (mute/unmute)
XF86AudioMicMute
    ~/scripts/mic.sh toggle

# mic up
super + Up
    ~/scripts/mic.sh up

# mic down
super + Down
    ~/scripts/mic.sh down

# mic toggle (alt key)
super + BackSpace
    ~/scripts/mic.sh toggle

#light up 
XF86MonBrightnessUp
	light -A 5 && pkill -RTMIN+20 i3blocks

#LIGHT DOWN 
XF86MonBrightnessDown
	light -U 5 && pkill -RTMIN+20 i3blocks

#light up 
# F12
# 		light -A 5 && pkill -RTMIN+20 i3blocks

#light down 
# F11
# 		light -U 5 && pkill -RTMIN+20 i3blocks

#dmenu toggle program 
# super + backslash
    # ~/scripts/lan_dmenu.sh && ~/scripts/toggle.sh

# run a program
super + d
    ~/scripts/lan_dmenu.sh && dmenu_run -c -l 20 -i -p "dmenu: "
    # ~/scripts/lan_dmenu.sh && rofi -show run
#lang 
super + space
    setxkbmap -query | grep -q "layout:\s\+us" && setxkbmap ara || setxkbmap us && pkill -RTMIN+15 i3blocks
#power 
super + Escape 
	/home/omar/scripts/power.sh
#play pause 
XF86AudioPlay
	playerctl play-pause
#play next 
XF86AudioNext
	playerctl next
#play pre
XF86AudioPrev 
	 playerctl previous
#play stop 
XF86AudioStop 
	playerctl stop
#mpd previous
ctrl + XF86AudioPrev 
    mpc prev 
#mpd next
ctrl + XF86AudioNext 
    mpc next
#mpd stop
ctrl + XF86AudioPlay
    mpc toggle 
#clipboard 
super + bracketleft
    ~/scripts/lan_dmenu.sh && clipmenu
#screeen record 
super + Print
	simplescreenrecorder
#mp3-youtube-dl 
super + y
    /home/omar/scripts/ytdmenu.sh
#screenshot 
Print
    /home/omar/scripts/flameshot.sh
#screenshot 
super + shift + s 
    flameshot gui -s -c
#hub 
super + o 
	~/scripts/hub.sh
#file manger 
shift + Return
    ~/scripts/filemanager.sh
    # alacritty -e zsh -ic 'nnn -Rrxl 5'
#open vim
super + v 
    alacritty -e nvim
    # alacritty -e lvim
# mail  
super + m
    thunderbird
# Pause button 
ctrl + Pause
    mpc toggle 
# Pause button 
ctrl + space
    mpc toggle 
# Pause button
Pause
	playerctl play-pause
# mpc stop 
ctrl + BackSpace 
	mpc stop 
# ocr 
super + e 
    ~/scripts/ocr.sh
# multi ocr
Insert
    ~/scripts/multi_ocr.sh
# emoji
super + period
    ~/scripts/lan_dmenu.sh && rofimoji --action copy --files ~/scripts/emoji -s neutral
    # ~/scripts/lan_dmenu.sh && bemoji -n
# rkeys
F10
    $HOME/scripts/keys.sh
# messenger
# super + m
#     messenger-nativefier
# switch
# super + shift + w 
#     ~/scripts/switch.sh
# obsidian
super + p
    obsidian 
# kill program
super + shift + k
    cd ~/scripts/dmenu_scripts/ && ./dm-kill
# web search
super + w
    ~/scripts/dm-websearch.sh
# yt with mpv
super + shift + p 
    mpv $(xclip -o)
# open dire
super + shift + o 
    ~/scripts/dmenu-places-menu.sh
